HOW TO RUN THIS APP (MiniMart)
================================

STEP 1 - Install Python and Node.js
------------------------------------------------------------
Easiest way: double-click "install-requirements.bat" in this folder.
It installs both automatically - just click "Yes" if Windows asks
for permission. When it finishes, RESTART THE COMPUTER.

(If that doesn't work, install them manually instead:
   Python:  https://www.python.org/downloads/
            - check "Add python.exe to PATH" during install
   Node.js: https://nodejs.org/ - pick the LTS version
 Then restart the computer.)

STEP 2 - Get the project
------------------------------------------------------------
1. Go to: https://github.com/engrhasnain/Mini
2. Click the green "Code" button, then "Download ZIP".
3. Find the downloaded ZIP file (usually in the Downloads folder),
   right-click it, and choose "Extract All...".
4. Open the extracted folder. You should see this file (HOW_TO_RUN.txt)
   along with "install-requirements.bat", "start-backend.bat" and
   "start-frontend.bat".

STEP 3 - Run it
------------------------------------------------------------
1. Double-click "start-backend.bat".
   A black window will open. The first time, it will take a minute
   to set itself up. Wait until you see:
        Uvicorn running on http://127.0.0.1:8000
   Leave this window open - do not close it.

   If it instead says Python is not installed, go back to Step 1.

2. Double-click "start-frontend.bat".
   Another black window will open. The first time, it will take a
   few minutes to set itself up. Wait until you see:
        Local:  http://localhost:3000
   Leave this window open too.

   If it instead says Node.js is not installed, go back to Step 1.

3. Open a web browser (Chrome/Edge) and go to:
        http://localhost:3000

The app should now be up and running!

To stop it later, just close both black windows.
To run it again another day, just double-click the two start-*.bat
files again (setup will be skipped since it's already done).
